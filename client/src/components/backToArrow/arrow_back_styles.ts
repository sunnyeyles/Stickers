import { createStyles, rem } from '@mantine/core'

export const arrowBackStyles = createStyles((theme) => ({
    arrowBackText: {
        writingMode: "vertical-lr",
        textOrientation: "upright",
        color: theme.colors.orange[6]
    }
}))