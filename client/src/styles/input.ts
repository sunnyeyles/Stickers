export const Input = {
  styles: (theme: any) => ({
    root: {
      '&:focus-within': {
        // backgroundColor: theme.colors.orange[9],
      },
      '&:hover': {
        // backgroundColor: theme.colors.primary[2],
      },
    },
  }),
}
