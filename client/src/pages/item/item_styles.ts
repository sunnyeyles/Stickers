import { createStyles, rem } from '@mantine/core'

export const itemStyles = createStyles((theme) => ({
    itemAmount: {
        width: rem(100)
    },
    error: {
        color: "#fa5252"
    }
}))
